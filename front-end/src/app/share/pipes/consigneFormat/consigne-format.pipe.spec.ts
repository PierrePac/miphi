import { ConsigneFormatPipe } from './consigne-format.pipe';

describe('ConsigneFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsigneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
